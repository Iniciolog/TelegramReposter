{
  "scripts": {
    "setup": "npm install && npm run db:push && npm run build",
    "deploy": "pm2 start ecosystem.config.js",
    "stop": "pm2 stop telegram-autoposter",
    "restart": "pm2 restart telegram-autoposter",
    "status": "pm2 status",
    "logs": "pm2 logs telegram-autoposter"
  }
}